/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['fbmdbvijfufsjpsuorxi.supabase.co', 'www.test.com', 'api.dicebear.com'],
  },
}

module.exports = nextConfig 